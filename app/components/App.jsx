import React from 'react';
import { SearchViewContainer, ForecastViewContainer } from '../containers';

const App = () => (
  <main className="application">
    <SearchViewContainer />
    <ForecastViewContainer />
  </main>
);

export default App;
